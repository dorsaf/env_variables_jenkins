pipeline {
    agent any
    stages {
        stage('Deploy - Development') {
            steps {
                 echo "the url is ${PORTAL_DEV_URL}"
                 echo "the url is ${PORTAL_STAGING_URL}"
                 echo "the url is ${PORTAL_URL}"
             }
        }
    }
}